# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/

# Build outputs
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# Environment files (never commit secrets)
.env
.env.local
.env.*.local
!.env.example

# Runtime AI-key overrides pasted via the UI (never commit)
.provider_overrides.json
services/ai/.provider_overrides.json

# Telegram credentials pasted via the UI (never commit)
.telegram-overrides.json
apps/api/.telegram-overrides.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
pip-wheel-metadata/

# Coverage
coverage/
.nyc_output/
htmlcov/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*.swn

# OS
.DS_Store
Thumbs.db

# Docker / local data volumes
.docker-data/
postgres-data/
redis-data/

# Backtest / validation scratch output (backtester.py --out, baseline_mc.py --out).
# Trained models and their metrics ARE tracked; per-run trade dumps are not —
# a single 1min run writes a ~2 MB trades.csv.
services/data/training/models/verdict_*/
services/data/**/backtest_out/

# Misc
.turbo/
.cache/
