# MT5 bridge — Windows ONLY (the MetaTrader5 package is Windows-only).
# Install inside a 64-bit Python venv on the same machine as the MT5 terminal.
MetaTrader5>=5.0.45
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.1
