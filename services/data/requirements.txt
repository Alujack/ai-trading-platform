asyncpg>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.1
# 2.2.2+ is the first pandas built against numpy 2.x; older 2.0.x crashes at import
# with "numpy.dtype size changed" when numpy>=2 is installed.
pandas>=2.2.2
# pandas-ta indicators — using the maintained fork (drop-in replacement for the
# abandoned `pandas-ta` PyPI package; import as `pandas_ta_classic`).
pandas-ta-classic>=0.4
# ONNX inference for the ported ml_xau LightGBM classifier (strategies/ml_xau.py).
# Imported lazily, so the worker still starts if this is missing.
onnxruntime>=1.17
# Training only (services/data/training/) — imported lazily so the live worker
# never needs them. lightgbm also needs the libgomp1 system package; see Dockerfile.
lightgbm>=4.3
onnxmltools>=1.12
skl2onnx>=1.16
