# services/data

Python data ingestion workers.

## Setup

```bash
cd services/data
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## Run

```bash
python -m src.worker
```
